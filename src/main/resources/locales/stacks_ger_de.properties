# Item name and lore localization.
#
# Inserting the block/item name into a text value is done with "${name}".
#
# Inserting the quantity of block/items into a text value is done with "${size}"

# Paths to values are in the form of:
#   stack.{compression_level}.name
# and
#   stack.{compression_level}.lore

# 9
stack.1.name = Eine Kollektion von ${name}
stack.1.lore = ${size} ${name}

# 81
stack.2.name = Ein Bündel mit ${name}
stack.2.lore = ${size} ${name}

# 729
stack.3.name = Ein Koffer mit ${name}
stack.3.lore = ${size} ${name}

# 6,561
stack.4.name = Eine Kiste mit ${name}
stack.4.lore = ${size} ${name}

# 59,049
stack.5.name = Eine Pallete mit ${name}
stack.5.lore = ${size} ${name}

# 531,441
stack.6.name = Ein Haufen von ${name}
stack.6.lore = ${size} ${name}

# 4,782,969
stack.7.name = Ein Vorrat von ${name}
stack.7.lore = ${size} ${name}

# 43,046,721
stack.8.name = Ein Berg von ${name}
stack.8.lore = ${size} ${name}

# 387,420,489
stack.9.name = Eine Galaxy von ${name}
stack.9.lore = ${size} ${name}

